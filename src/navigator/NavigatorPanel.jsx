import React from 'react';

const NavigatorPanel = props => {
  const styles = {
    color: '#444',
    fontSize: '14px',
    position: 'absolute'
  };

  return <div style={styles}>Tell me</div>;
};

export default NavigatorPanel;
